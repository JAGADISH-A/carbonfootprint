# =============================================================================
#  Carbon Intelligence Platform — Application Configuration
#  All sensitive values are read from environment variables.
#  No secrets are hardcoded.
# =============================================================================

spring:
  application:
    name: carbon-intelligence-platform

  # ── Jackson (JSON serialisation) ────────────────────────────────────────────
  jackson:
    serialization:
      write-dates-as-timestamps: false
    deserialization:
      fail-on-unknown-properties: false
    default-property-inclusion: non_null

  # ── File Upload ─────────────────────────────────────────────────────────────
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB



  # ── Actuator ─────────────────────────────────────────────────────────────────
  # Note: Custom health endpoint is at /api/v1/health
  # Spring Actuator health is at /actuator/health
  management:
    endpoints:
      web:
        exposure:
          include: health,info,metrics
    endpoint:
      health:
        show-details: when-authorized

# =============================================================================
#  AI Provider Selection
#  Controls which DocumentParser bean is loaded.
#  Values: groq (default)
#  Override via: AI_PROVIDER=groq  or  -Dai.provider=groq
# =============================================================================
ai:
  provider: ${AI_PROVIDER:groq}

# =============================================================================
#  Carbon Platform — Custom Configuration Namespace
# =============================================================================
carbon:

  # ── Google Firestore ─────────────────────────────────────────────────────────
  firestore:
    project-id: ${GCP_PROJECT_ID}
    activities-collection: ${FIRESTORE_ACTIVITIES_COLLECTION:activities}
    raw-documents-collection: ${FIRESTORE_RAW_DOCS_COLLECTION:raw_documents}
    carbon-assessments-collection: ${FIRESTORE_CARBON_COLLECTION:carbon_assessments}

  # ── File Upload ──────────────────────────────────────────────────────────────
  upload:
    supported-mime-types: ${CARBON_SUPPORTED_MIME_TYPES:image/jpeg,image/png,image/webp,application/pdf}

  # ── Groq AI ───────────────────────────────────────────────────────────
  groq:
    api-key: ${GROQ_API_KEY:}
    receipt-model: ${GROQ_RECEIPT_MODEL:openai/gpt-oss-20b}
    coach-model: ${GROQ_COACH_MODEL:llama-3.3-70b-versatile}
    base-url: ${GROQ_BASE_URL:https://api.groq.com}
    timeout-seconds: ${GROQ_TIMEOUT:60}

  # ── OCR Configuration ────────────────────────────────────────────────────────
  ocr:
    # Active provider: paddle | cloud_vision (future)
    provider: ${OCR_PROVIDER:paddle}
    paddle:
      base-url: ${PADDLE_OCR_URL:http://localhost:8001}
      timeout-seconds: ${PADDLE_OCR_TIMEOUT:30}

  # ── AI Coach Cache ──────────────────────────────────────────────────────────
  coach:
    cache:
      enabled: ${COACH_CACHE_ENABLED:true}
      ttl-minutes: ${COACH_CACHE_TTL_MINUTES:60}

  # ── CORS ─────────────────────────────────────────────────────────────────────
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000}

  # ── Companion App Release ─────────────────────────────────────────────────────
  companion:
    version: ${COMPANION_VERSION:1.0.0}
    download-url: ${COMPANION_DOWNLOAD_URL:https://github.com/anomalyco/carbonfootprint/releases/download/v1.0.0/carbonwise-v1.0.0.apk}
    file-size-bytes: ${COMPANION_FILE_SIZE:15728640}
    release-date: ${COMPANION_RELEASE_DATE:2026-06-21}
    release-notes:
      - ${COMPANION_NOTE_1:Improved SMS filtering and categorization}
      - ${COMPANION_NOTE_2:Faster data synchronization with dashboard}
      - ${COMPANION_NOTE_3:Better battery optimization for background sync}
      - ${COMPANION_NOTE_4:Bug fixes and stability improvements}

  # ── Google OAuth 2.0 ─────────────────────────────────────────────────────────
  security:
    oauth2:
      # Used for server-side Gmail API calls (Phase 2)
      client-id: ${GOOGLE_CLIENT_ID:}
      client-secret: ${GOOGLE_CLIENT_SECRET:}

# =============================================================================
#  OpenAPI / Swagger UI (disabled in production via profile override)
# =============================================================================
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: alpha
    tags-sorter: alpha
  # Disable in production: set SPRINGDOC_ENABLED=false or use production profile
  enabled: ${SPRINGDOC_ENABLED:true}

# =============================================================================
#  Logging
# =============================================================================
logging:
  level:
    com.carbonfootprint: INFO
    com.google.cloud: INFO
    org.springframework.security: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
