# Base URL for the Spring Boot backend API.
# During local development, point this to the local backend port.
# For production, point this to the deployed Google Cloud Run backend URL.
VITE_API_URL=http://localhost:8080
